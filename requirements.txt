# BenchMind runtime dependencies.
# UTF-8. (1.x shipped this file as UTF-16 from a PowerShell redirect, which
# made `pip install -r requirements.txt` fail on Linux and macOS.)

# --- core ---
numpy>=1.24
psutil>=5.9
threadpoolctl>=3.1
requests>=2.28

# --- api and dashboard ---
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0

# --- optional: cloud sync (db/) ---
# Not imported at startup; db/client.py degrades to offline mode without these.
python-dotenv>=1.0
# supabase>=2.0

# --- optional: GPU benchmarking (benchmarks/gpu/) ---
# Verified against pyopencl 2026.1.4 on Windows with the NVIDIA CUDA and Intel
# Graphics OpenCL runtimes. Vendor runtimes ship with the GPU driver, so
# normally nothing else is needed.
# pyopencl>=2023.1
